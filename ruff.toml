# Config for the remaining Python files in the repo (e.g. demo/video_gen/).
# The MCP server itself is TypeScript; there is no other Python source tree.
target-version = "py313"
line-length = 120

[format]
quote-style = "double"
indent-style = "space"
